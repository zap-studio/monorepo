comment:
  layout: "reach,diff,flags,tree"
  require_changes: true

status:
  project:
    default:
      target: auto
      threshold: 1%
  patch:
    default:
      target: 85%
      threshold: 1%

ignore:
  - "apps/docs/**"
  - "**/dist/**"
  - "**/*.config.*"
