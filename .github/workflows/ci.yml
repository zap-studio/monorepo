name: CI

on:
  pull_request:
    branches: ["**"]
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  PACKAGE_FILTER: ./packages/*

jobs:
  build-packages:
    name: Build Packages
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: ./.github/actions/setup-pnpm

      - name: Build packages
        run: pnpm --filter "$PACKAGE_FILTER" run build

  build-docs:
    name: Build Docs
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: ./.github/actions/setup-pnpm

      - name: Build docs
        run: pnpm --filter docs run build

  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup pnpm
        uses: ./.github/actions/setup-pnpm

      - name: Build packages
        run: pnpm --filter "$PACKAGE_FILTER" run build

      - name: Run check
        run: pnpm run check

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: ./.github/actions/setup-pnpm

      - name: Build packages
        run: pnpm --filter "$PACKAGE_FILTER" run build

      - name: Install browser
        run: pnpm exec playwright install --with-deps chromium

      - name: Run tests
        run: pnpm run test

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: ./.github/actions/setup-pnpm

      - name: Build packages
        run: pnpm --filter "$PACKAGE_FILTER" run build

      - name: Install browser
        run: pnpm exec playwright install --with-deps chromium

      - name: Run coverage
        run: CI=1 pnpm run test:coverage

      - name: Upload test results to Codecov
        if: ${{ always() }}
        uses: codecov/codecov-action@v5
        with:
          disable_search: true
          files: ./coverage/junit.xml
          fail_ci_if_error: false
          report_type: test_results
          use_oidc: true
          verbose: true

      - name: Upload coverage to Codecov
        if: ${{ always() }}
        uses: codecov/codecov-action@v5
        with:
          disable_search: true
          files: ./coverage/lcov.info
          fail_ci_if_error: false
          use_oidc: true
          verbose: true

  publish-check:
    name: Publish Check
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: ./.github/actions/setup-pnpm

      - name: Build packages
        run: pnpm --filter "$PACKAGE_FILTER" run build

      - name: Install browser
        run: pnpm exec playwright install --with-deps chromium

      - name: Run publish checks
        run: pnpm run format:check && pnpm run lint && pnpm run test && pnpm --filter "$PACKAGE_FILTER" run prepublishOnly
