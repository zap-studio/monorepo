name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-packages:
    name: Build Packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Setup nub
        uses: ./.github/actions/setup-nub
      - name: Build packages
        run: nub run --filter "./packages/*" build

  build-docs:
    name: Build Docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Setup nub
        uses: ./.github/actions/setup-nub
      - name: Build docs
        run: nub run --filter docs build

  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup nub
        uses: ./.github/actions/setup-nub
      - name: Build packages
        run: nub run --filter "./packages/*" build
      - name: Run check
        run: nub run check

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Setup nub
        uses: ./.github/actions/setup-nub
      - name: Build packages
        run: nub run --filter "./packages/*" build
      - name: Install browser
        run: nub exec playwright install --with-deps chromium
      - name: Run tests
        run: nub run test

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Setup nub
        uses: ./.github/actions/setup-nub
      - name: Build packages
        run: nub run --filter "./packages/*" build
      - name: Install browser
        run: nub exec playwright install --with-deps chromium
      - name: Run coverage
        run: CI=1 nub run test:coverage
      - name: Upload test results to Codecov
        if: ${{ always() }}
        uses: codecov/codecov-action@v7
        with:
          disable_search: true
          files: ./coverage/junit.xml
          fail_ci_if_error: false
          report_type: test_results
          use_oidc: true
          verbose: true
      - name: Upload coverage to Codecov
        if: ${{ always() }}
        uses: codecov/codecov-action@v7
        with:
          disable_search: true
          files: ./coverage/lcov.info
          fail_ci_if_error: false
          use_oidc: true
          verbose: true
