export type BlogServerPluginConfig = {
  BASE_PATH: string;
  DATA_DIR: string;
  MAX_BLOG_POSTS_IN_FOOTER: number;
};

// TODO: change that
export type BlogClientPluginConfig = {
  nothingForNow: null;
};
