import {
  _SuccessPage,
  _SuccessPageProps,
} from "../../../../../../zap/payments/pages";

export default async function SuccessPage({ searchParams }: _SuccessPageProps) {
  return <_SuccessPage searchParams={searchParams} />;
}
