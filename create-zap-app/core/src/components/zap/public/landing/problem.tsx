export function ProblemSection() {
  return (
    <div className="mx-auto max-w-[58rem] space-y-4 px-4 text-center md:px-6">
      <h2 className="text-3xl font-bold tracking-tighter sm:text-4xl md:text-5xl">
        Building Next.js apps shouldn&apos;t be this hard
      </h2>
      <p className="text-muted-foreground mx-auto max-w-[85%] md:text-xl">
        You&apos;re tired of setting up the same boilerplate code for every
        project. Authentication, database, styling, deployment configs —
        it&apos;s time-consuming and frustrating. And when deadlines loom, the
        pressure only mounts.
      </p>
    </div>
  );
}
