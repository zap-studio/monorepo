---
"@zap-studio/fetch": patch
"@zap-studio/validation": minor
---

Extract shared Standard Schema validation utilities into `@zap-studio/validation` and update `@zap-studio/fetch` to depend on them.


