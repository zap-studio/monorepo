---
"@zap-studio/validation": minor
"@zap-studio/permit": patch
---

Add a reusable synchronous Standard Schema validator helper in `@zap-studio/validation` and refactor `@zap-studio/permit` to use it for resource schema validation in `createPolicy`.


