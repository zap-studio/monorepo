---
title: Overview
description: What Zap Studio is, what each package covers, and where agent skills live.
---

import { Tab, Tabs } from "fumadocs-ui/components/tabs";

Zap Studio is a collection of framework-agnostic TypeScript packages for core app capabilities: typed HTTP calls, authorization policy checks, retry policies, schema validation, and webhook processing.

Our goal is to provide composable building blocks that stay portable across stacks while remaining strict about type safety and runtime correctness.

## Package Overview

| Package | What it covers |
| --- | --- |
| [`@zap-studio/fetch`](/docs/packages/fetch) | Type-safe HTTP requests with schema-validated responses |
| [`@zap-studio/permit`](/docs/packages/permit) | Declarative authorization policies and permission checks |
| [`@zap-studio/retry`](/docs/packages/retry) | Composable retry policies with fixed and exponential backoff |
| [`@zap-studio/validation`](/docs/packages/validation) | Standard Schema utilities and consistent validation errors |
| [`@zap-studio/webhooks`](/docs/packages/webhooks) | Webhook routing, signature verification, and payload validation |

## Agent Skills

Skills are split by package and maintenance workflow so agents can load focused guidance for the task at hand.

Install them with the Vercel Skills CLI:

<Tabs items={["npm", "yarn", "pnpm", "bun", "deno"]}>
  <Tab value="npm">

```bash
npx skills add zap-studio/monorepo
```

  </Tab>
  <Tab value="yarn">

```bash
yarn dlx skills add zap-studio/monorepo
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx skills add zap-studio/monorepo
```

  </Tab>
  <Tab value="bun">

```bash
bunx skills add zap-studio/monorepo
```

  </Tab>
  <Tab value="deno">

```bash
deno run -A npm:skills add zap-studio/monorepo
```

  </Tab>
</Tabs>
